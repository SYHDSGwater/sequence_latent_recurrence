# 2026-09-18 — 优先进行公开权重状态干预

## Previous belief

T²MLR 的论文正面结果值得检验，但不足以认定公开权重实现持续 latent reasoning。

## New evidence

公开入口未找到 1.7B retrofit 或对应逐题 traces；982M checkpoint 配置包含反馈融合与状态残差积累两种作用。当前代码存在缺失导入，已用最小适配器与官方单步函数对照处理。

## Belief update

不对有效性作正负更新。更明确了可检验对象与实现语义，以及不能从公开 base checkpoint 推断 1.7B 数学结果。

## Decision

continue：在 5 H100 小时预算内优先 reset/donor 恢复曲线、carry 消融及 clean-KV 路径阻断。此决定只安排研究；尚未执行 GPU 实验。

## Rationale / Next action

先固定 held-out 数据来源和样本，再进行完整权重加载、BF16 预检与吞吐测量。若通过，执行 E-T2-001；将原始 traces 与 manifest 作为证据，不把 CPU 单元测试填成假说实验结果。

## 执行后更新 — 当前实验完成

用户指定 RTX 5090D 实例后，已完成 32 篇 validation 主实验、32 篇 test 确认，以及复用主实验 32 篇文章的探索性 KL/state-distance 补测。完整结果见 C-T2-5090 与 `06_reports/t2mlr_5090_results.md`。

信念更新：冻结模型依赖反馈路径；加法累积的小收益得到重复；单次干预的远期平均 NLL 代价近零，但非负差异显示小幅持续残留。因此更支持短时特征复用/可重建状态，未获得持续有益推理状态的证据。主实验的显著当步平均伤害未在确认显著复现。

Decision: stop 本轮已定义实验，不继续消耗剩余预算。所有 GPU 实验进程已结束，原本开机的实例保持原状态，数据已回收到本地。

下一阶段若要检验 latent reasoning，应先定义需要跨步保存中间变量的可验证任务，并设计正确/错误语义状态替换与 attention 历史控制；不能仅凭这轮通用文本消融判断架构优劣。

## 用户授权扩展后的更新 — 135M/10B 与更大样本已完成

用户随后要求扩大样本、加入数学题和 135M/10B checkpoint。现已完成两个 checkpoint 各 256 篇文章与 256 道数学题参考解答的五条件实验，四个正式运行全部完成并独立重算验证，见 C-T2-EXPANDED 和 `06_reports/t2mlr_expanded_results.md`。

信念更新：旧状态累积的小幅平均预测收益，以及单次重置远期平均代价较小，均跨 checkpoint 与文本分布成立。绝对预测差异仍存在，不能推断状态信息彻底消失。数学 teacher forcing 没有建立自由解题或持续推理机制；982M 更大的路径依赖也未对应更大的累积收益。

Decision: stop 本轮已固定的四组实验，保留原始结果；下一研究方向应 pivot 到解答内部语义状态替换及 attention 可重建性控制，而非继续单纯扩大相同配方样本。正式四组约 15.8 分钟，不含下载和吞吐试跑。GPU 任务已结束，原本运行的实例保持开机。
