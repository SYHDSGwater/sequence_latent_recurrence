# Expanded frozen-checkpoint mechanism study — 2026-09-18

Fixed before inspecting expanded outcomes. Instance pro-78730289ac36, single 32GB RTX 5090 family GPU. No training.

- Two pinned checkpoints: 982M/50B revision 5061964f36236336153106d4b11155e09ee2daaa; 135M/10B revision 2bbdb5540e56aaf09b2a5e80e63fcf57d629d3db. The latter changes model size, training budget and recurrent layer span together; no isolated scaling claim.
- 256 unique WikiText-103 train articles with >=513 tokens, seeded random permutation, first eligible unique titles; first 513 tokens, intervention at input position 128, continuous controls scored positions 32–511. Train refers to the public dataset split, not a training run in this study. Possible pretraining contamination is not ruled out.
- 256 seeded random eligible MATH-500 problems, all subjects/levels allowed. Eligibility: prompt <=512 tokens and reference solution >=2 tokens. Report all exclusions. Prompt is `Problem:\n{problem}\nSolution:\n`; encode prompt and solution separately, concatenate, use at most 256 solution tokens. Perturbation occurs immediately before processing the last prompt token, affecting prediction of the first solution token. Score only reference solution targets. This is reference-solution likelihood, not autonomous solving or answer accuracy.
- Both tokenizers must produce identical selected token IDs. Freeze input JSONL and SHA256 before GPU evaluation. Sort selected examples by length only for efficient right padding. Causal right padding occurs after scored tokens and never enters metrics.
- Arms: normal; single zero reset with naturally evolving KV; single zero reset with clean-prefix KV restored each token; carry_off (extra recurrent residual removed); zero_every (incoming state zero each step). All run on the same examples. Full trained-weight BF16 parity with the upstream exact inference method is required per checkpoint.
- Signed delta NLL relative to normal, document/problem weighted. Also mean absolute tokenwise delta to reveal cancellation. Single intervention horizons: lag 0, 1–7, 8–31, 32–127. Include an example in a horizon only if the complete horizon exists; publish per-horizon n. Primary hypotheses: continuous carry removal increases NLL; far signed average reset cost is small (predefined practical interval ±0.01 nat/token). Far math conclusions apply only to solutions with >=128 scored tokens. Zero_every is a pathway dependency stress test, not an architecture ablation retraining comparison.
- Bootstrap 2,000 resamples of independent documents/problems, fixed seed; pointwise 95% CIs, not multiplicity-corrected discovery tests. Do not pool tokens as independent units. Compare models on matched IDs, report descriptive differences; population generalization remains limited by datasets, checkpoint count and teacher forcing/full KV.
- Default batch 16, complete paired batches only. Overall remote command hard cap 14,400 seconds, per-cell cap 5,400 seconds. If capped or failed, report incomplete status, never silently substitute a smaller selected set. Existing results retained separately.

## Throughput amendment, before inspecting expanded outcome summaries

The initial 135M batch-16 execution used about 1.8 GB of GPU memory at about 21% utilization, taking 64.9 seconds for its first paired batch. Increase batch size to 64, retain that incomplete attempt separately as `135m_wiki_batch16_pilot`, and rerun the full selected set. No sample, metric, arm or stopping criterion changes. These arms have no cross-example donor coupling. Data downloads required a SHA256-verified segmented-transfer fallback; all three large assets matched pinned upstream LFS hashes.
